# AzaLoop Launch — Social Media Content Kit

---

## LinkedIn Post

**Hook:**
AI 编码助手已经改变了一切。但为什么你的项目还是完不成？

**Body:**

2024 年，我们欣喜地发现 AI 可以写代码了。
2025 年，我们发现 AI 写的代码"差不多能用"。
2026 年，AzaLoop 让 AI 真正**完成**代码。

AzaLoop 不是新的 AI 编码助手。它是让你的现有 AI 编码助手（Cursor、Claude Code、OpenCode、Trae 等 25+ 客户端）进化的引擎。

一条命令：
```
npx @azaloop/cli init
```

然后，对 AI 说："帮我创建一个 React 应用。"

AzaLoop 不会停下来等你回复。它会**自动**完成：

需求 → PRD 生成 → 任务拆解 → 编码 → 测试 → 安全扫描 → 文档 → 交付

不需要 API Key。不需要配置 LLM。你已经在付费的 AI 助手，现在可以做以前做不到的事。

**CTA:**
链接在评论区 👇
#AI #AzaLoop #软件开发 #Cursor #ClaudeCode

---

## Twitter/X Thread (8 tweets)

**Tweet 1/8:**
AI 可以写代码了。但 AI 能"完成"代码吗？

大多数 AI 编程工具的问题：你给它一个需求，它写几个文件，等你回复"继续"，再写几个文件，再等……

项目永远停留在"差不多做完"。

**Tweet 2/8:**
AzaLoop 解决了这个问题。

它不是新的 AI 编码助手。它是让你的现有 AI 编码助手（Cursor、Claude Code、OpenCode 等）从"对话工具"进化为"自主开发引擎"的东西。

一条命令：
```npx @azaloop/cli init```

**Tweet 3/8:**
然后，对 AI 说："帮我创建一个 React 应用。"

AzaLoop 不会停下来等你回复。它自动完成：

需求 → PRD 生成 → 任务拆解 → 编码 → 测试 → 安全扫描 → 文档 → 交付

整个过程，你不需要回复"继续"。

**Tweet 4/8:**
最独特的设计：不需要 API Key。

AzaLoop 运行在你的 AI 编码助手内部，完全复用你已付费的模型：

Cursor → 复用 Cursor 订阅模型
Claude Code → 复用 Claude 订阅
Trae → 复用 Trae 内置模型
GitHub Copilot → 复用 Copilot 订阅

零成本，零配置。

**Tweet 5/8:**
AzaLoop 有 5 个阶段的全自动流水线：

1. open — PRD 生成（14 章结构化模板）
2. design — 架构设计、任务拆解
3. build — TDD 编码（测试先行）
4. verify — 5 级质量门禁（lint → test → build → security → acceptance）
5. archive — 文档归档、经验提取

任何一关不通过，自动重试。

**Tweet 6/8:**
三层记忆系统：

工作记忆 — 当前会话状态
情景记忆 — 跨会话开发历史
语义记忆 — 代码约定和最佳实践

你在一个项目中踩过的坑，在另一个项目中会被自动避开。

**Tweet 7/8:**
25+ 客户端支持：

T1 全功能：Cursor、Claude Code、OpenCode、Trae
T2 部分：VS Code + Copilot、Cline、Windsurf、Continue、Claude Desktop、Comate（百度）、Workbuddy（字节）、Qwen Code（阿里）
T3 基础：Aider、Zed、Goose、Hermes 等

**Tweet 8/8:**
GitHub: https://github.com/coeasy/azaloop

MIT 许可，开箱即用。

你的 AI 已经在付费。现在，让它做点真正的事情。

---

## Reddit Post (r/programming / r/Cursor)

**Title:**
AzaLoop: 让 AI 编码助手从"对话工具"进化为"自主开发引擎" — 25+ 客户端，零 API Key

**Body:**

最近在用 AI 编码助手的过程中，我发现了一个根本性的问题：AI 可以写代码，但不能"完成"代码。

你给它一个需求，它写几个文件，等你回复"继续"，再写几个文件……项目永远停在"差不多做完"。

所以我写了 AzaLoop。

它不是新的 AI 编码助手。它是让你现有的 AI 编码助手（Cursor、Claude Code、OpenCode、Trae 等）从"对话工具"进化为"自主开发引擎"的引擎。

**核心特性：**

1. 宿主 LLM 零配置 — 复用你已付费的模型，不需要 API Key
2. PRD 驱动 — 14 章结构化需求文档，不是模糊的 Prompt
3. 五阶段全自动流水线 — open → design → build → verify → archive，每个阶段有质量门禁
4. 反射记忆 — 三层记忆系统，跨会话持续学习
5. 25+ 客户端支持 — Cursor、Claude Code、VS Code + Copilot、Trae、Windsurf、Comate（百度）、Qwen Code（阿里）等

**使用方式：**

```
npx @azaloop/cli init
```

然后在 AI 助手中说："帮我创建一个 React 应用。"

AzaLoop 不会停下来等你回复。它会自己完成所有事情。

**GitHub:** https://github.com/coeasy/azaloop

MIT 许可，欢迎试用和贡献。

---

## 微信公众号文章（标题 + 开头）

**标题选项：**

1. AzaLoop：让 AI 编码助手从"对话工具"进化为"自主开发引擎"
2. 你的 AI 已经在付费，为什么不让它"完成"代码？
3. 一条命令，让 Cursor/Claude Code/OpenCode 变成自主开发引擎

**开头：**

2024 年，AI 编码助手横空出世。开发者们欣喜地发现，AI 可以在聊天中写代码了。

但很快，所有人都遇到了同一个瓶颈：AI 可以写代码，但不能"完成"代码。

你给它一个需求，它写几个文件，然后等你回复"继续"，再写几个文件……项目永远停留在"差不多做完"的状态。

这不是 AI 的能力问题。这是 AI 编程的**范式问题**。

AzaLoop 改变了这个范式。
