# /aza-ship — Release / ship

Activate **ship** role.

1. `aza_quality(action=check)` must pass
2. `aza_finish(action=ship)`
3. Summarize artifacts under `.aza/` and openspec archive notes
4. Never force-push main
